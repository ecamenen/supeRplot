% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_venn_diagram.R
\name{plot_venn_diagram}
\alias{plot_venn_diagram}
\title{Venn diagram}
\usage{
plot_venn_diagram(
  x,
  width_text = 30,
  width_label = 30,
  colour = get_colors(),
  cex = 1,
  cex_main = cex * 6,
  n_max = 4,
  vjust_label = 0.5,
  ratio = 0.1,
  label = TRUE,
  percent = TRUE,
  element = TRUE
)
}
\arguments{
\item{x}{X}

\item{width_text}{Integer for the maximum length of the subtitle(s).}

\item{width_label}{Integer for the maximum length of the labels.}

\item{colour}{Color or vector of colors for the violin and boxplot.}

\item{cex}{Double for the magnification factor for the text relative to the
default.}

\item{cex_main}{Double for the magnification factor for the subtitles
relative to the default.}

\item{n_max}{Integer for the maximum number of element to show.
After this threshold, only their statistics will be visible.}

\item{vjust_label}{Double for the vertical justification of the labels
(in [0, 1]).}

\item{ratio}{Double for the width scale.}

\item{label}{Boolean to toggle the display of the labels.}

\item{element}{Boolean to toggle the display of the elements.
If disabled, only their statistics will be visible.}

\item{percentage}{Boolean to toggle the display of the percenatge of the
elements.}
}
\value{
A ggplot object.
}
\description{
Visualize a venn diagram with the element of in each set and their
intersection.
}
\examples{
library(magrittr)
library(RColorBrewer)

x <- lapply(
    seq(3),
    function(x) {
        runif(10, 1, 10) \%>\%
            round() \%>\%
            letters[.] \%>\%
            unique() \%>\%
            paste("Element", .)
    }
) \%>\%
    set_names(paste("Dataset", LETTERS[seq(3)]))

# Default parameters
plot_venn_diagram(x)

# Advanced parameters
plot_venn_diagram(
    x,
    width_text = 8,
    width_label = 5,
    colour = brewer.pal(3, "Reds"),
    cex = 1.2,
    cex_main = 1.2 * 6,
    n_max = 2,
    vjust_label = 0.75,
    ratio = 0.5,
    label = FALSE,
    element = FALSE
)

}
